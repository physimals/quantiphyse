# Example config file for volume calculation process

OutputFolder: output
Debug: False

Processing:
  - Load:
  
  - CalcVolumes:
        output-name: roi_vols

  - SaveExtras:
        roi_vols:

Cases:
    CalcVolumes:
        Folder:   data_autogen
        Load:  
          data:
            testdata_4d.nii.gz:
          rois:
            testdata_mask.nii.gz: mask


