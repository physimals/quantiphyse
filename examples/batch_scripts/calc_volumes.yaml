# Example config file for volume calculation process

OutputFolder: test/examples_output
Debug: False

Processing:
  - Load:
  
  - CalcVolumes:
        output-name: roi_vols

  - SaveExtras:
        roi_vols:

Cases:
    CalcVolumes:
        Folder:   test/data_autogen
        Load:  
          data:
            testdata_4d.nii.gz:
          rois:
            testdata_mask.nii.gz: mask


